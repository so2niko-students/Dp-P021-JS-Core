let l, w, h;
// do {
//     l = Number(prompt('Enter length', 1));
//     w = Number(prompt('Enter width', 1));
//     h = Number(prompt('Enter height', 1));
// } while (l < 0 || l > 1000 || w < 0 || w > 1000 || h < 0 || h > 1000);

// while (true) {
//     l = Number(prompt('Enter length', 1));
//     w = Number(prompt('Enter width', 1));
//     h = Number(prompt('Enter height', 1));
//     if (l > 0 || l <= 1000 || w > 0 || w <= 1000 || h > 0 || h <= 1000) {
//         break;
//     }
// }
console.log(l, w, h);